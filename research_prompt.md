# 调研提示词：创业者利润计划工具 — 开源方案与架构选型

> **使用说明**：将以下内容发送给 GPT-4 (Deep Research) 或 Perplexity (Pro Search)，建议开启 "深度研究" 模式。

---

## 一、产品目标

我要搭建一个面向 **初创创业者** 的轻量级 Web 工具。核心理念：

> **用户只管填"花多少钱"，系统负责算"怎么赚回来"**

实际创业者往往只知道成本（房租、工资、材料费等），不知道定什么价、卖多少件才能回本。这个工具的核心价值不是"生成一张静态利润表"，而是**帮用户探索：在给定的成本结构下，什么样的定价策略和销售规模能让项目成立**。

### 支持双创业模式

工具需同时支持两种截然不同的创业形态，用户进入后先选择模式，后续所有计算和展示自动适配：

| | 模式 A：产品型 | 模式 B：服务型 / 一人公司 (OPC) |
|------|------|------|
| **典型场景** | 实体店、电商、摆摊、批发 | 设计、咨询、开发、个人工作室、自由职业 |
| **销售单位** | 件、杯、单、份 | 小时、项目、客户、月费 |
| **变动成本** | 材料/进货/包装，每件有明确成本 | 近乎为零或极低（获客成本、平台抽成） |
| **固定成本** | 房租、工资、水电、运营费 | 软件订阅、营销、云服务、社保 |
| **启动资金** | 设备、装修、证照、首批库存 | 电脑、软件、注册费、品牌建设 |
| **定价逻辑** | 市场比价 + 成本加成 | 时薪制 / 项目报价 / 月费制 |
| **规模瓶颈** | 受店面/产能/库存限制 | **受个人精力限制**（月计费小时有硬上限） |
| **核心推导变量** | 单价 × 销量 | 时薪/客单价 × 客户数/计费小时 |

**技术上为什么不难**：两种模式的底层计算公式完全一致——只是变量的**名字和单位**不同。用一个计算引擎 + 两套参数配置即可实现，额外开发量约 20%。

**服务型模式的特殊处理**：
- 系统需自动提示"单人产能天花板"（典型月计费小时约 80-120 小时）
- 时薪/客单价的推导更依赖 AI 辅助推荐（服务定价缺乏市场比价基准）
- 支持"项目制"和"月费制"两种收入模型切换

---

## 二、产品核心架构

### 五大计算引擎（按计算依赖排序）

```
① 启动资金预测 ──→ ④ 销售与成本计划
     │                    │
     └──→ ② 盈亏平衡推导 ←┘
              │
              ▼
          ③ 销售收入预测
              │
              ▼
          ⑤ 现金流量计划
```

| 模块 | 做什么 | 关键计算逻辑 |
|------|--------|-------------|
| **① 启动资金预测** | 用户填项目名+行业+规模，系统估算需要多少启动资金 | 一次性投入（设备/装修/证照）+ 运营备用金（N个月固定成本） |
| **② 盈亏平衡推导（核心）** | 已知成本结构，反推什么单价×什么销量能回本 | 盈亏平衡销量 = 固定成本 / (单价 - 单位变动成本)；生成可探索的 (P, Q) 组合空间 |
| **③ 销售收入预测** | 基于推导出的价格和销量，预测不同场景下的收入 | 保守/中性/乐观三档场景，按月展开 |
| **④ 销售与成本计划** | 按月展开完整的收入-成本-利润计算 | 月收入 - 月变动成本 - 月固定成本 → 月净利润；标注第几个月开始盈利 |
| **⑤ 现金流量计划** | 关注实际现金进出，预警断流风险 | 期初现金 + 现金流入（含回款周期）- 现金流出（含付款周期）→ 期末现金 |

### 三张输出报表

| 报表 | 回答的问题 | 核心字段 |
|------|-----------|---------|
| **利润表 (P&L)** | 能不能赚钱？ | 营业收入 → 毛利 → 营业利润 → 净利润（含毛利率、净利率） |
| **现金流量表** | 钱会不会断？ | 按月展开：期初现金 + 流入 - 流出 = 期末现金，含预警线 |
| **盈亏平衡分析图** | 卖多少才回本？ | X轴=销量，Y轴=金额，总收入线与总成本线的交叉点 |

### 用户最小输入（≤12 个字段，按模式动态切换标签）

两套模式共用同一套 Schema，但字段标签和含义随模式切换。用 `|` 分隔不同模式下的显示文案。

| 字段 | 产品型文案 | 服务型文案 | 必填 | 用于模块 |
|------|-----------|-----------|------|---------|
| 项目名 | 如"写字楼咖啡店" | 如"独立UI设计工作室" | √ | 全部 |
| 行业/品类 | 餐饮/零售/服装 | 设计/开发/咨询 | √ | ①②③ |
| 初始投资估算 | 设备+装修+证照+首批库存 | 电脑+软件+注册费+品牌建设 | 可选 | ①⑤ |
| 月度固定成本 | 租金+工资+水电等 | 软件订阅+营销+社保+云服务 | √ | ②③④⑤ |
| 单位变动成本 | 每件产品的材料/进货成本 | 每客户的获客成本/平台抽成 | √ | ②③④ |
| 定价字段 | 每件产品卖多少钱 | 时薪 / 每客户收费 / 每项目报价 | 可选 | ②③④ |
| 销量字段 | 每月能卖多少件 | 每月计费小时数 / 客户数 | 可选 | ②③④ |
| 目标月利润 | 想赚多少 | 想赚多少 | 可选 | ②③ |
| 收入到账周期 | 现结/月结/N天 | 项目里程碑付款/预付款/尾款 | 可选 | ⑤ |
| 供应商/平台账期 | 供应商结账周期 | 平台结算周期（如有） | 可选 | ⑤ |
| 团队人数/产能 | 员工数 | 每月可投入计费小时数（默认160h） | 可选 | ①② |
| 预计开业月份 | 开业时间 | 开始接单时间 | 可选 | ⑤ |

---

## 三、调研范围与输出要求

请围绕以下 5 个维度展开深度调研，每个维度给出 **具体可落地的推荐方案**，而不是泛泛列举。

---

### 维度 1：现有开源方案调研
**目标**：找到可以直接复用或二次开发的开源项目。

请搜索并评估：
- 是否存在开源的 "利润计算器 / P&L 生成器 / 创业财务规划 / 现金流管理" 类项目？
- 特别关注 GitHub 上 Star > 50 的相关项目，按成熟度排序
- 对每个项目，评估：技术栈、是否支持中文、是否可自部署、授权协议是否允许商用、二次开发难度
- 如果不存在直接匹配的项目，哪些 "个人财务管理" 类开源项目（如 Maybe、Firefly III、Ghostfolio）的架构或计算引擎可以复用？
- 是否存在带有 **盈亏平衡分析 / Break-even Analysis** 功能的开源工具？

**输出格式**：表格对比，含项目名、GitHub Stars、技术栈、核心功能、商用许可、二次开发评估

---

### 维度 2：技术架构与组件选型
**目标**：设计一套 "最小可行产品（MVP）" 技术栈，优先使用成熟开源组件。

请分别推荐以下层面的最佳选择，并说明推荐理由：

| 层级 | 需求描述 | 推荐方案 |
|------|---------|---------|
| **前端** | 首页模式选择（产品型/服务型）→ 动态表单（字段标签按模式切换）→ 交互式盈亏探索面板（滑块/图表）→ 三张报表展示，需移动端友好 | 推荐框架及 UI 组件库 |
| **计算引擎** | 五大模块的计算逻辑，尤其是盈亏平衡反向推导、多场景模拟 | 纯前端计算 or 后端 API？推荐方案 |
| **图表可视化** | 盈亏平衡分析图（散点/折线图）、现金流趋势图、利润表柱状图 | 推荐开源图表库 |
| **报表生成** | 生成可导出（PDF/Excel）的利润表、现金流量表 | 推荐开源报表/导出库 |
| **数据持久化** | 用户数据存储（可选登录）、多项目保存 | 推荐数据库 or 纯本地存储方案 |
| **部署** | 低成本部署，支持自定义域名 | 推荐部署平台与方案 |

额外要求：
- 推荐是否有 "无代码 / 低代码" 方案可以更快交付（如 Airtable + 公式、NocoDB、Appsmith + 自定义公式等）
- 如果要接入 AI（如根据项目名自动推荐行业参考定价区间、智能识别成本项），推荐哪些开源 AI 组件或 API？

---

### 维度 3：计算模型与数据结构设计（最核心）
**目标**：明确全部计算逻辑的完整数学模型。

请根据标准财务管理逻辑，给出：

#### 3.1 双模式统一公式链

核心洞察：两种模式的数学骨架完全一致，仅变量含义不同。

**通用基础公式**：
```
利润 = (单位收费 U - 单位变动成本 VC) × 业务量 V - 固定成本 FC

产品型：单位收费 = 单价 P，         业务量 = 销量 Q，     VC = 材料成本
服务型：单位收费 = 时薪/客单价 R，   业务量 = 计费小时/客户数 H， VC = 获客成本
```

**统一盈亏平衡推导**：
```
盈亏平衡业务量 V₀ = FC / (U - VC)
盈亏平衡收入 R₀ = FC / (1 - VC/U)

给定目标月利润 T：
  → 所需业务量 V = (FC + T) / (U - VC)
  → 所需单位收费 U = VC + (FC + T) / V

统一约束：U > VC、V > 0
```

**服务型特有约束 — 产能天花板**：
```
月最大计费小时 ≈ 总工作小时 × 计费比例
               ≈ 160h × 50%-75%（含商务、管理、学习等非计费时间）
               ≈ 80-120 可计费小时/月

月收入天花板 = 时薪 × 最大计费小时
             = 时薪 × 120（上限）

系统需在用户设定目标超过天花板时给出预警：
"按你当前的时薪假设，即使满负荷工作（120计费小时），月收入上限为 ¥XX,XXX，请调整时薪或接受现实约束"
```

**服务型收入模型子模式**：
```
时薪制：月收入 = 时薪 × 计费小时
项目制：月收入 = 月均项目数 × 项目均价
月费制：月收入 = 客户数 × 月费 × 续费率
```

**① 启动资金预测公式（按模式）**
```
产品型：
  启动资金 = 设备采购 + 装修费用 + 证照办理 + 加盟费 + 首批库存 + 运营备用金
  运营备用金 = 月度固定成本 × 建议预留月数（通常 3-6 个月）

服务型：
  启动资金 = 电脑设备 + 软件授权 + 公司注册 + 品牌建设(网站/logo) + 运营备用金
  运营备用金 = 月度固定成本 × 建议预留月数（通常 3-6 个月，自由职业建议 6 个月）
```

**② 盈亏平衡推导公式（统一核心）**
```
同上述通用公式，为五种计算模式的核心
```

**③ 销售收入预测公式**
```
统一：月收入 = U × V
多场景：保守(系数0.7) / 中性(1.0) / 乐观(1.3)
服务型附加：标记"产能天花板"线在图表上
```

**④ 销售与成本计划（P&L）公式**
```
统一公式，同前述
```

**⑤ 现金流量计划公式**
```
统一公式，服务型额外考虑：
- 项目制收款节奏（预付款-里程碑-尾款）
- 平台结算周期（如Upwork/Toptal的周结/月结）
```

#### 3.2 数据模型设计
- 推荐的数据模型 Schema（JSON 或 数据库表结构）
- 需要包含：Project（项目，含 `mode: 'product' | 'service'` 字段）、CostItem（成本项）、Scenario（场景/方案）、MonthlyProjection（月度预测）
- 服务型 Project 需额外字段：`serviceRevenueModel`（hourly/project/retainer）、`maxBillableHours`（产能上限，默认 120）
- 如何处理 "按月" vs "按年" vs "按项目生命周期" 三种计算模式

#### 3.3 反向推导的交互逻辑
核心设计问题：当用户只填了成本，不知道单价和销量时，系统如何引导用户探索可行方案？请给出交互流程设计建议，例如：
- 方案 A：默认展示"盈亏平衡曲面"（不同P×不同Q组合的利润矩阵），用户通过滑块探索
- 方案 B：用户先设目标利润，系统给出多条(P, Q)路径
- 方案 C：结合行业参考数据，给出推荐区间
- 推荐采用哪种组合方案，并说明理由

**特别关注服务型模式的交互差异**：
- 服务型用户对"每客户收费"或"时薪"的感知远弱于产品型用户对"单价"的感知
- 服务型推导更依赖 AI 辅助——根据用户填写的技能/行业推荐定价区间
- 产能天花板（~120计费小时/月）需作为硬约束在 UI 上可视化展示
- 建议默认展示"不同时薪下，需要多少客户/多少计费小时才能回本"的对比表
- 支持用户在"时薪制 / 项目制 / 月费制"之间快速切换，实时看到收入变化

---

### 维度 4：快速集成与商业化路径
**目标**：找到最大化速度、最小化开发成本的集成方案。

请调研：
- 是否存在成熟的 "嵌入式财务计算器" SaaS API（如计算税金、折旧、利润率等），可直接调用？
- 推荐哪些支付/订阅集成方案（用于工具本身商业化收费）？
- 有没有现成的 Google Sheets / Excel 盈亏平衡分析模板可以作为 MVP 先行验证需求，再转为 Web 应用？
- 如何实现多语言（至少中英文）？推荐的开源 i18n 方案
- 市场上同类工具（如 LivePlan、Enloop、ProjectionHub）的技术栈和定价策略分析
- 是否有提供 "行业基准数据" 的 API 或数据集（如各行业平均毛利率、平均定价区间等），用于辅助推导？
- 对于服务型模式，是否有提供"自由职业者各岗位时薪基准数据"或"各行业顾问/设计师报价区间"的数据源？
- 推荐的服务定价参考平台或社区（如 Upwork 时薪数据、脉脉/小红书上的报价讨论等）

---

### 维度 5：参考案例与最佳实践
**目标**：学习类似项目的技术实现路径。

请搜索并提供：
- GitHub 上使用 React/Vue + 财务计算逻辑的开源项目案例（不限于利润表，特别关注含盈亏平衡计算的项目）
- Hacker News / Indie Hackers 上有过讨论的 "创业财务计算器" 案例及技术选型
- 是否有开源项目用到了 "规则引擎" 来处理复杂的财务计算逻辑？（如 json-rules-engine、Nools）
- 推荐的技术参考书籍或在线文档（关于财务计算、会计引擎设计、盈亏平衡分析）
- 关于 **交互式数据可视化** 的 React/Vue 组件推荐（滑块+图表联动、多场景对比图等）
- 是否有针对 **自由职业者 / 一人公司** 的财务规划开源工具或社区案例？
- 服务型商业模式（时薪制 vs 项目制 vs 月费制）的收入建模参考案例

---

## 四、约束条件

- **交付物**：Web 应用（PWA 优先），移动端友好
- **目标用户**：中国市场的初创创业者（但架构需支持国际化扩展）
- **时间预算**：MVP 开发时间控制在 2 周以内（单人开发）
- **成本预算**：基础设施月成本控制在 ¥200 以内
- **授权合规**：所有推荐组件需明确标注开源协议，优先 MIT / Apache 2.0
- **MVP 阶段不包含**：资产负债表、多用户协作、AI 智能识别发票

---

## 五、期望的回复结构

请按以下结构组织回答：

```
1. 执行摘要（3-5 句，给出最强推荐路径）
2. 现成开源方案对比（表格）
3. 推荐技术架构（分层说明 + 架构图描述）
4. 计算模型与反向推导设计（五大模块公式链 + 反向推导交互方案）
5. 数据模型设计（Schema + 最小字段集）
6. 第三方 API / 服务推荐（含行业基准数据来源）
7. MVP 开发路径建议（分 Phase 1/2/3，每阶段的工作内容和预计耗时）
8. 商业化建议（定价策略、目标客群、差异化优势）
9. 风险与注意事项
```

---

## 六、附录：参考上下文

我当前的技术背景与现状：
- 具备全栈开发能力，偏好 Python/Node.js 后端 + React/Vue 前端
- 已有参考文档《制定你的利润计划》（一份面向创业者的利润规划指南）
- 当前工作目录为空项目，从零开始搭建
- 优先使用现有开源方案，避免重复造轮子
- **核心差异化**：本工具不是简单的利润计算器，而是帮不知道收入项的创业者"反向探索"可行方案
